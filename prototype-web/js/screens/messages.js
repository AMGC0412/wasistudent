const Messages = {
  render() {
    document.getElementById('messages-content').innerHTML = '<div class="empty">' + I.chat + '<h3>Sin mensajes</h3><p>Cuando inicies un chat, aparecerá aquí.</p></div>';
  }
};
